{
  "name": "AnunciosLoc Dev",
  "dockerComposeFile": ["docker-compose.yml"],
  "service": "app",
  "workspaceFolder": "/workspaces/AnunciosLoc",
  "shutdownAction": "stopCompose",
  "postCreateCommand": "bash -lc 'cd backend && npm install && cd ../Frontend && npm install && bash -lc \"until mysqladmin ping -h db --silent; do sleep 1; done\" && cd ../backend && npm run db:init'",
  "remoteUser": "node",
  "customizations": {
    "vscode": {
      "extensions": ["dbaeumer.vscode-eslint", "esbenp.prettier-vscode"]n    }
  }
}
